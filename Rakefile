require "bundler/gem_tasks"
require 'active_record'
require 'base'


namespace :db do 
	
	desc "Prep the database in order to update it with new content stored
	in the data folder."
	task :clean do

	end

	desc "Load the new data in the data files located in the data folder"
	task :load do
	end

	desc "Analyze the new data entered into the database"
	task :analyze do
	end



end

